# backend/requirements.txt

# --- FastAPI (웹 서버) ---
fastapi
uvicorn[standard]
fastapi-cors        # CORS 오류 해결
python-multipart    # 파일 업로드 처리

# --- LangChain (AI 로직) ---
langchain           # LangChain 코어 라이브러리
langchain-core      # 프롬프트, Runnable 등 핵심 모듈
langchain-text-splitters # 텍스트 분할기 (RecursiveCharacterTextSplitter)

# --- LangChain (연동 모듈) ---
langchain-ollama    # Ollama 연동 (OllamaLLM, OllamaEmbeddings)
langchain-chroma    # ChromaDB 연동 (Chroma)

# --- 데이터 처리 ---
chromadb            # 벡터 DB (파일 기반)
pymupdf             # PDF 파일 로드 (PyMuPDFLoader)