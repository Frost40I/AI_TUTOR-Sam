백엔드 : 터미널에서 cd backend 명령어로 backend 폴더로 이동한 뒤 venv\Scripts\activate 명령어로 가상환경 활성화 후 
uvicorn app.main:app --host 127.0.0.1 --port 8000 --reload 입력
프론트 : 터미널에서 cd frontend로 fronted로 이동 후 npm install로 노드 모듈 다운로드 후 npm start
